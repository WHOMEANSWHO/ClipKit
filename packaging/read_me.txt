ClipKit

Send ClipKit.exe only. Double-click it. No Python install.

The first run installs ClipKit as a Windows app (Start menu, Settings → Apps).

If Windows SmartScreen appears: More info → Run anyway.

Close OBS (including the tray icon) before you click Apply.
